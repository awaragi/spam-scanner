# Tasks

## 1. Scaffold and tooling

- [x] 1.1 Run the NestJS 12 CLI (`nest new`) to generate `app/server`, choosing ESM, and confirm it defaults to Vitest and oxlint on Node 24 — verify: `npm test --workspace=app/server` runs the scaffold's own sample test successfully. **Done.**
- [x] 1.2 Verify decorator metadata resolves under the scaffold's Vitest setup: add a minimal `@Injectable()` with a constructor-injected dependency and a `Test.createTestingModule` test that resolves it — verify: the test passes; if DI resolution fails, add `unplugin-swc` to the Vitest config only (keep tsc for the build) per design.md's Risk mitigation, then re-verify. **Done — needed `unplugin-swc`** (scaffold default failed the smoke test; fixed, verified, throwaway files removed).
- [x] 1.3 Delete the scaffold's sample `app.controller.ts`/`app.service.ts` and their spec files; rename its generated e2e folder to `test/api/` (left empty until `5-server-api-auth`) — verify: `app/server/src/` contains only `main.ts` and `app.module.ts`. **Done.**
- [x] 1.4 Create the full folder skeleton from design.md D1 as empty module directories: `config/`, `logging/`, `domain/{classification,ai,sender-lists,scanning,state,utils}`, `infrastructure/{imap,rspamd,ai,state,mailboxes}`, `application/{scanning,training,folders}`, `runtime/` — verify: the directory tree matches D1. **Done** (`.gitkeep` per empty dir).
- [x] 1.5 Add dependency-cruiser to `app/server`, configured for the layering rule in design.md D3 (`api` → `runtime` → `application` → `infrastructure`/`domain`/`config`; `infrastructure` may import `domain`/`config`; `domain` imports only `domain`), wired into `npm run lint --workspace=app/server` — verify: lint passes on the still-empty skeleton (nothing to violate yet). **Done**, rules confirmed to actually fire via a temporary violating file before removal.

## 2. Configuration and logging

- [x] 2.1 Write `config/app-config.schema.ts`: zod `configGroups` for the app-setting keys and the mailbox-connection keys per design.md D5 and the `server/configuration` spec — verify: `app-config.schema.spec.ts` validates a fixture env per key group, asserts defaults, and asserts that two simultaneous validation failures are reported together in one error
- [x] 2.2 Wire `config/config.module.ts` as an `@Global` module on `@nestjs/config` with Standard Schema validation, exposing typed sections in `config/app-config.ts` — verify: a test resolves each typed config section from a fixture env via `Test.createTestingModule`
- [x] 2.3 Write `config/mailbox-settings.defaults.ts`: the camelCase per-mailbox settings object (folders, `scanRead`, `scanInitialState`, `processingMode` and labels, thresholds, AI escalation thresholds, `aiEnabled`) — verify: a test asserts its shape and that every default value matches terminal's current `config.ts` defaults for the same setting
- [x] 2.4 Wire `logging/logging.module.ts` on `nestjs-pino`, configured asynchronously from `ConfigService` with `bufferLogs: true`, preserving terminal's level/format/component-filter/redaction behavior (`logging-levels` capability) — verify: a test confirms `IMAP_PASSWORD`, `RSPAMD_PASSWORD`, and `AI_API_KEY` are redacted from an emitted log line
- [x] 2.5 Port the subset of terminal's `ConfigSchema.superRefine` cross-field checks (`terminal/src/lib/core/config.ts`) that still apply to the trimmed schema: (a) `AI_MODEL` required when `AI_ENABLED=true`; (b) `AI_API_KEY` required when `AI_ENABLED=true` and `AI_BASE_URL` is the default OpenAI endpoint; (c) `MAILBOX_IMAP_ALLOW_INSECURE=true` required when `MAILBOX_IMAP_TLS=false` — this one is mandatory, not optional: the proposal commits to reproducing `imap-transport-security` unchanged, and that capability's spec requires this check. Do NOT port the escalation-threshold check (those fields are now code-only per-mailbox defaults, not env) or the notify-address check (moot now that `MAILBOX_ID` is always a validated email) — verify: a test for each of the three ported checks confirms it fires, and a test confirms all three can surface together in one validation error alongside an unrelated bad field (extending 2.1's "multiple failures reported together" test)

## 3. Env scripts

- [x] 3.1 Write `scripts/generate-env-example.ts`, rendering `app/server/.env.example` from `app-config.schema.ts`'s `configGroups` (reuse the ported `domain/utils/env-file.ts` rendering logic) — verify: running it produces the file, and a drift test added to `app-config.schema.spec.ts` fails if the committed file doesn't match what the schema currently renders **Done.**
- [x] 3.2 Write `scripts/migrate-env.ts --input <path> --output <path>` per design.md D10 and the `server/configuration` spec's migration requirement: keep app-setting values unchanged, map `IMAP_*` → `MAILBOX_IMAP_*` and `FOLDER_STATE` → `MAILBOX_STATE_FOLDER`, derive `MAILBOX_ID` from an email-shaped `IMAP_USER` or else `IMAP_NOTIFY_ADDRESS` (empty + warning if neither matches), drop every other key, fill schema defaults for anything missing, and print only key names and counts — verify: unit tests run it against fixture env files only (never a real `.env`) covering a typical full source file, a source file with no email-shaped value available, and an assertion that no test output or log line contains a fixture secret's actual value **Done.** Also fixed by orchestrator: nest build was broken (missing mailparser type declarations, from task 4.2's port) - ported terminal's src/lib/types/mailparser.d.ts shim; scripts run via direct node+.ts-extension execution rather than dist/, amended in design.md D10.

## 4. Domain layer (pure, ported from terminal)

- [x] 4.1 Port `domain/classification/` (`spam-classifier.ts`, `error-classifier.ts`, dropping the `.service` suffix per design.md D2) with their tests — verify: ported tests pass with the same behavioral assertions as terminal's, only import paths changed **Done** — also wrote domain/sender-lists/sender-lists.ts as a same-named dependency 4.3 also owns canonically; reconciled: current file is 4.3's, confirmed behaviorally identical to 4.1's version (both independently chose to strip logging), 64/64 tests intact.
- [x] 4.2 Port `domain/ai/` (`ai-content.ts`, `ai-error-reason.ts`, `ai-failure-tracker.ts`) with tests — verify: same **Done** — its port imported a no-op logger shim + duplicated two utils files that 4.5 also owns canonically; reconciled by the orchestrator: shim deleted, ai-content.ts's logging calls stripped to match the rest of domain/ (logging is I/O per D1; belongs at the application layer).
- [x] 4.3 Port `domain/sender-lists/` (`sender-lists.ts`, `list-diff.ts`) with tests — verify: same
- [x] 4.4 Port `domain/scanning/` (`scan-progress.ts`) and `domain/state/` (`state-format.ts`, keeping terminal's exact state-key constants `scanner`, `rspamd-whitelist-map`, `rspamd-blacklist-map`) with tests — verify: same, plus a test confirming the ported state-format functions still produce byte-identical message bodies to terminal's for the same input
- [x] 4.5 Port `domain/utils/` (`concurrency.ts`, `email.ts`, `email-parser.ts`, `mailboxes.ts`, `ai-content.util.ts`, `env-file.ts`) with tests — verify: same

## 5. Infrastructure layer

- [x] 5.1 Split `imap.client.ts` per design.md D7: `infrastructure/imap/imap-connection.factory.ts` (`newClient`/`safeLogout`/`withSession`), preserving the exact TLS/`doSTARTTLS` decisions from `imap-transport-security`, with ported tests — verify: ported tests assert the same TLS/STARTTLS behavior as terminal's `imap.client` tests **Done.**
- [x] 5.2 Extract `infrastructure/imap/mailbox.gateway.ts` (open/search/fetch/move/labels/append, all by UID) and `infrastructure/imap/message.mapper.ts` (`processMessage`/`processMessageHeaders`) with ported tests — verify: same behavioral assertions as terminal's for these operations **Done.**
- [x] 5.3 Write `infrastructure/imap/folder.resolver.ts` per design.md D6: takes a mailbox's folder settings plus a live session and returns a resolved `MailboxFolders` object, without mutating any shared state — verify: a test resolves two different mailboxes with different delimiters/settings in the same process and confirms neither resolution affects the other **Done.**
- [x] 5.4 Port `infrastructure/imap/inbox.watcher.ts` (the IDLE code) and its tests, unused for now — verify: ported tests pass; confirm nothing under `application/` or `runtime/` imports it yet **Done.**
- [x] 5.5 Port `infrastructure/rspamd/rspamd.gateway.ts` (`checkEmail`/`learnHam`/`learnSpam`) with tests, reading rspamd config via an injected config section rather than importing `config` directly — verify: ported tests pass constructing the gateway via `Test.createTestingModule` **Done.**
- [x] 5.6 Port `infrastructure/ai/ai.gateway.ts` (`buildSystemPrompt`/`buildUserContent`/`classifyEmail`) with tests, config via injection — verify: same **Done.** Orchestrator added back error/debug logging via Nest's Logger (matching terminal and 5.5's rspamd.gateway.ts pattern) - the agent had dropped it citing D1's domain-purity rule, which does not apply to infrastructure/.
- [x] 5.7 Write `infrastructure/state/scanner-state.repository.ts` and `sender-list.repository.ts` (ported from `state-manager.client.ts`), preserving the append-then-delete write and highest-UID-wins read (`state-manager` capability) with tests — verify: ported tests pass with identical behavioral assertions **Done.**
- [x] 5.8 Write `infrastructure/mailboxes/mailbox.repository.ts` and `mailbox.ts` per the `server/mailbox-registry` spec: returns a list built from the mailbox-connection config section, exactly one entry, id = the configured email, and exposes that id as the rspamd user with no way to override it — verify: a test asserts the returned list has exactly one mailbox and its rspamd-user value equals its id **Done.**

## 6. Application layer (use-cases)

- [x] 6.1 Write `application/mailbox-session.ts`: the session shape carrying the mailbox, its open IMAP connection, resolved settings, resolved folders, and a mailbox-scoped logger (design.md D4) — verify: a test builds one from test doubles and confirms every field is accessible **Done.**
- [x] 6.2 Write `application/folders/folder-init.service.ts`, ported from `init.controller.ts`, taking a `MailboxSession` instead of `ctx` — verify: ported `init.controller` test cases pass via `Test.createTestingModule().overrideProvider(...)`, same behavioral assertions as terminal's **Done** (constructed directly + vi.mock, not overrideProvider, since mailbox.gateway.ts is function-based like terminal's clients, not a DI class - noted, acceptable).
- [x] 6.3 Write `application/scanning/` (`scan.service.ts` plus its steps: `pending-messages.step.ts`, `rspamd-check.step.ts`, `ai-classification.step.ts`, `disposition.step.ts` merging label-apply/folder-move/spam-move, `sender-list-lookup.step.ts`), ported from `scan.controller.ts` and `controllers/steps/`, taking a `MailboxSession` — verify: ported `scan.controller` and step test cases pass with unchanged behavioral assertions **Done.** Also correctly wired the new per-mailbox settings.aiEnabled opt-out (AND'd with the global AiConfig.enabled), and dropped only the AI-failure-alert plumbing per design's explicit non-goal. Test fixtures are duplicated across the three 6.2/6.3/6.4 spec files rather than shared, to avoid parallel-dispatch collisions - a reasonable consolidation follow-up, not urgent.
- [x] 6.4 Write `application/training/` (`rspamd-training.service.ts`, `sender-list-training.service.ts`), ported from `train.controller.ts`, `sender-list-training.controller.ts`, and their steps — verify: ported test cases pass with unchanged behavioral assertions **Done** - preserved a real, deliberate-looking asymmetry between the two services (rspamd training swallows failures per batch-processing-resilience; sender-list training rethrows, uncovered by that spec) rather than normalizing it away.

## 7. Runtime (minimal loop) and app assembly

- [x] 7.1 Write `runtime/` per design.md D9: for each mailbox from the registry, run folder init once, then on every `SCAN_INTERVAL` tick run the training jobs followed by scan-until-drained, one fresh session per job, log-and-continue on a job failure (never exit the process), and stop cleanly (no new job starts) on `OnApplicationShutdown` — verify: a test drives the loop with a short interval and a stubbed job that fails once, confirming the loop logs the failure and continues, and confirms a triggered shutdown prevents the next job from starting **Done.** First pass shared one session per tick, contradicting design.md D9's explicit "each job gets its own session" - a bug in the orchestrator's own dispatch prompt, not the agent's fault. Fixed: each training call and each scan pass now opens and closes its own fresh session, matching terminal orchestrator.ts's runStep() pattern, with per-job (not per-tick) log-and-continue and per-job shutdown checks.
- [x] 7.2 Wire `main.ts` (`NestFactory`, the pino logger, `bufferLogs`, shutdown hooks) and `app.module.ts` (importing config, logging, infrastructure, application, and runtime modules) — verify: `npm run start --workspace=app/server` boots against a fixture env without crashing and exits cleanly on `SIGTERM` **Done.** Created the seven per-layer modules design.md D1 asks for (`RspamdModule`, `AiModule` — also the `AiFailureTracker` singleton registration, `MailboxesModule`, `ScanningModule`, `TrainingModule`, `FoldersModule`, `RuntimeModule`); skipped an `imap.module.ts` since every IMAP file is plain functions, not `@Injectable()` classes — nothing to provide (per the task's own judgment-call carve-out). `enableShutdownHooks()` needed `{ useProcessExit: true }`, not just the bare call, to actually exit 0 on `SIGTERM` (Nest's default re-raises the signal to itself post-cleanup, exiting 128+signal instead) — verified by building and booting `dist/main.js` against a fixture env with an unreachable IMAP host: it logs the expected bootstrap-failure and continues, serves an HTTP 404 on the configured port, and exits 0 within ~2s of `SIGTERM`.

## 8. Docker and compose

- [x] 8.1 Write `app/server/Dockerfile` with a build stage and a slim runtime stage. Build from the **repo root** as context (not `app/server` alone), copying the root `package.json`/`package-lock.json` plus `app/server/package.json`, then `npm ci --workspace=app/server --omit=dev` before `nest build` — this is a workspace-aware install from the start, avoiding the lockfile gap `terminal/`'s Dockerfile hit in `1-monorepo-restructure`. Run as the non-root `node` user — verify: `docker build -f app/server/Dockerfile .` (from the repo root) succeeds and the resulting image's entrypoint starts without an immediate crash against a fixture env **Done.** Build revealed a real bug in app/server/package.json (missing imapflow/openai/email-addresses/mailparser - phantom deps masked by npm workspace hoisting from terminal), fixed by the orchestrator, then re-verified: 379MB image, boots, serves, docker stop completes in 0.2s.
- [x] 8.2 Add a `server` service under a `server` profile to the root `docker-compose.yml`, with `build: { context: ., dockerfile: app/server/Dockerfile }`, joining the existing project network, depending on healthy rspamd — verify: `docker compose --profile server config` resolves without error **Done.** Verified scoped to the server service only (unscoped docker compose config inlines every service's env_file, including spam-scanner's real root .env - avoided printing that).

## 9. End-to-end verification

- [x] 9.1 Run the full `app/server` unit suite and confirm every ported test passes with unchanged behavioral assertions — verify: green test run **Done.**
- [x] 9.2 Run dependency-cruiser across the now-populated tree — verify: zero layering violations **Done.**
- [x] 9.3 Confirm `app/server/.env.example` is committed and matches the schema — verify: the drift test from 3.1 passes **Done** - explicitly re-ran the drift test in isolation (1/1 pass).
- [x] 9.4 Confirm the server can take over scanning an existing mailbox from terminal without losing state, without actually running either against a real mailbox (per the same caution as `1-monorepo-restructure`'s deferred smoke test) — verify: a code-level test asserting the server's state-key constants and state-message byte format are identical to terminal's for the same input **Done** - covered by task 4.4's byte-identical parity tests in state-format.spec.ts, re-confirmed (19/19 pass).
