# Tasks

## 1. Enable per-user Bayes in the shared rspamd config (D1)

- [x] 1.1 Add `per_user = true;` to `rspamd/config/classifier-bayes.conf` (alongside the existing `backend = "redis";`), with a short comment recording why (per-mailbox Bayes models) and the key-layout consequence (the Redis prefix becomes `%s%l%r`, i.e. per-user keys are the global keys with the mailbox id appended) so the next operator/migration step has context. Verify: the file is valid rspamd UCL (no syntax error) — e.g. `docker compose -f docker-compose.yml -f docker-compose.base.yml config` still resolves, or a local rspamd `configtest` if available; at minimum confirm the two directives are well-formed. This is a shared-infra config change, verified structurally here; behavioral effect (per-user models) is confirmed by the manual verification in task 4/5.

## 2. RspamdGateway sends the mailbox id as the `Deliver-To` user (D2)

- [x] 2.1 In `app/server/src/infrastructure/rspamd/rspamd.gateway.ts`, extend `buildHeaders` to accept an optional `user` and set `headers['Deliver-To'] = user` only when `user` is a non-empty string (omit the header entirely otherwise, so no-user calls are unchanged — D4's fallback). Keep the existing envelope handling intact. Verify with a unit test (extend `rspamd.gateway.spec.ts`) that `buildHeaders` includes `Deliver-To` when a user is given and omits it when not.
- [x] 2.2 Add an optional `user?: string` parameter to `checkEmail`, `learnHam`, and `learnSpam`, passing it through to `buildHeaders`. Do not change any other behavior (status handling, already-learned handling, envelope for `checkEmail`). Verify with unit tests (mocked `fetch`): each of the three methods sends the `Deliver-To: <user>` header when a user is passed, and sends no `Deliver-To` header when called without one (guarding the existing callers/tests that pass none).

## 3. Thread `session.mailbox.id` through the scoring and training call sites (D3)

- [x] 3.1 In `app/server/src/application/scanning/rspamd-check.step.ts`, pass `session.mailbox.id` as the new `user` argument to `this.rspamd.checkEmail(raw, rspamdEnvelope, session.mailbox.id)`. Verify with a unit test (or extension of the existing check-step spec) that the mailbox id is forwarded to `checkEmail` as the user for a scored message.
- [x] 3.2 In `app/server/src/application/training/rspamd-training.service.ts`, replace the two user-less `LearnFn` instance fields (`trainSpam`/`trainHam`) with per-run closures built inside `runSpam(session)`/`runHam(session)` that capture the session's mailbox id: `(raw) => this.rspamd.learnSpam(raw, session.mailbox.id)` and the `learnHam` equivalent, passed into `trainMessages`. Do not change `trainMessages` or the `LearnFn` type (the user is captured in the closure, not added to the signature) — `rspamd-training.step.ts` is untouched. Verify with unit tests (extend `rspamd-training.service.spec.ts`) that a spam-training run calls `learnSpam` with `session.mailbox.id`, and a ham-training run calls `learnHam` with `session.mailbox.id`.

## 4. One-off global-to-first-mailbox corpus migration script (D5)

- [x] 4.1 Add a dependency-free POSIX shell script `bin/migrate-bayes-per-user.sh` (executable, `set -euo pipefail`) that migrates the global Bayes corpus to the first mailbox's per-user keys by COPYing (never moving) each global Bayes key to `<key><MAILBOX_ID>`, per the verified key layout (D-context/D5). It SHALL: (a) read the first mailbox id from `MAILBOX_ID` (error out if unset); (b) print a prominent reminder to back up `${SPAM_SCANNER_DATA}/redis/` first, and require confirmation before any write; (c) discover global Bayes keys via `redis-cli ... --scan --pattern 'RS*'` (via `docker compose exec -T redis redis-cli` when run against the compose stack; make the redis-cli invocation overridable via an env var like `REDIS_CLI`), excluding keys already per-user (those ending with an `@`-containing mailbox id); (d) default to a **dry-run** that prints each planned `key -> key<MAILBOX_ID>` copy without executing, and only perform the copies under an explicit `--apply` flag using Redis `COPY` (never `RENAME`/`DEL`, so the source corpus is preserved — D4). Verify: `bash -n bin/migrate-bayes-per-user.sh` parses; running the script in its default (dry-run) mode with `MAILBOX_ID` set prints the planned copies and performs no writes. A live end-to-end run against real Redis with `--apply` is a manual operator step (it touches the live corpus and requires a backup) and is **deferred** — documented, not executed here.
- [x] 4.2 Add a short usage/README note for the script (either a header comment block in the script itself or a pointer from the relevant docs) covering: back up Redis first, dry-run then `--apply`, that it copies (not moves) so it is safe/re-runnable, and that it is a one-off that can be deleted after cutover. Verify the note names the backup path and the dry-run/`--apply` distinction.

## 5. Verification

- [x] 5.1 Run the full server test suite (`npm test --workspace=app/server`) and `npm run lint --workspace=app/server` (oxlint --type-aware + dependency-cruiser) and confirm both pass with no regressions — especially that existing gateway/check-step/training tests that call rspamd without a user still pass (the `Deliver-To` header must be absent, not empty, when no user is given). Also `npm run build --workspace=app/server` compiles. **Done:** 692/692 tests across 62 files pass; dependency-cruiser reports no violations (167 modules); `nest build` compiles. New tests assert `Deliver-To` is present with a user and absent (not empty) without one.
- [x] 5.2 Manual/operator verification (deferred, documented — no live rspamd+Redis in this repo, same limitation as prior changes' manual steps): after enabling `per_user = true` and deploying the gateway change, confirm against a live rspamd 3.14 + Redis that (a) a `/checkv2` and `/learnspam`/`/learnham` with `Deliver-To: <mailbox id>` create/select `RS...<mailbox id>` keys in Redis (`redis-cli --scan --pattern 'RS*'`), (b) a request without `Deliver-To` still scores against the default corpus, and (c) the migration dry-run's planned copies match the actual global keys before running `--apply`. Left for the operator; the automated tests cover the header-sending behavior, and the rspamd mechanics were verified against the 3.14 docs during design. **Deferred:** requires a live rspamd 3.14 + Redis (touches the real Bayes corpus), out of reach in this repo. The migration script's dry-run was exercised with a stubbed `redis-cli` and correctly plans copies while skipping already-per-user (`@`) keys and performing no writes.
- [x] 5.3 Run `openspec validate 6-per-user-bayes --strict` and confirm it reports valid after implementation. **Done:** reports valid.
