# Tasks

## 1. State write durability (3.1)

- [x] 1.1 In `src/lib/state-manager.js`, reorder `writeScannerState` to capture the existing matching UID(s) via `search` first, then `imap.append` the new state message, then delete only the previously-captured UID(s) (not a fresh post-append search). Verify: existing `writeScannerState` behavior tests still pass and a new test asserts `append` is called before `messageDelete` (call-order assertion on a mocked `imap`).
- [x] 1.2 Apply the same append-before-delete reordering to `writeMapState`. Verify: same call-order assertion pattern as 1.1, on a mocked `imap`.
- [x] 1.3 Add a unit test for `writeScannerState`/`writeMapState`: when the mocked `imap.append` rejects, `messageDelete` is never called (old state message is preserved). Verify: test passes.
- [x] 1.4 Change `readScannerState`'s state-message selection from `results[0]` to the maximum UID in `results` (`Math.max(...results)`). Verify: new unit test with a mocked `search` returning multiple UIDs (e.g. `[5, 12, 8]`) asserts the message fetched is UID `12`.
- [x] 1.5 Add a `mailboxPath` parameter to `readScannerState(imap, defaultState, mailboxPath)`. When `defaultState` is used (no state message found) and `mailboxPath` is provided, call `imap.status(mailboxPath, {uidNext: true})` and set `last_uid = uidNext - 1` when `uidNext > 1`, otherwise `last_uid = 0`; log a `warn` in both default-state cases noting a default was used. Verify: unit tests cover uidNext > 1 (non-zero last_uid, warn logged), uidNext === 1 (last_uid 0, warn logged), and `mailboxPath` omitted (falls back to `last_uid: 0` from the caller-supplied default, current behavior preserved, e.g. for `src/admin/read-state.js` which calls `readScannerState(imap)` with no default at all and must keep throwing when no state exists).
- [x] 1.6 Update `scan-workflow.js`'s `runScan` to pass the resolved inbox folder path as `mailboxPath` to `readScannerState`. Verify: existing `scan-workflow.test.js` default-state tests updated/passing; new test confirms `readScannerState` is called with the inbox path.
- [x] 1.7 `grep -rn "readScannerState(" src/` and confirm every call site (`scan-workflow.js`, `src/admin/read-state.js`) is compatible with the new signature (positional, backward-compatible when `mailboxPath` is omitted). Verify: no other call sites found beyond the two accounted for above; `npm test` green.

## 2. Folder delimiter resolution (4.2)

- [x] 2.1 Add a `splitFolderParts(path)` helper to `src/lib/utils/mailboxes-utils.js` (extracted from `collectFoldersToCreate`'s existing split regex) and refactor `collectFoldersToCreate` to use it, with no behavior change. Verify: existing `mailboxes-utils.test.js` tests still pass unmodified.
- [x] 2.2 Create `src/lib/utils/folder-resolver.js` exporting `resolveFolders(imap)` (async: gets the server delimiter via the existing `getImapDelimiter` logic in `imap-client.js` — export it from there — then builds `{FOLDER_INBOX, FOLDER_SPAM, FOLDER_SPAM_LOW, FOLDER_SPAM_HIGH, FOLDER_TRAIN_SPAM, FOLDER_TRAIN_HAM, FOLDER_TRAIN_WHITELIST, FOLDER_TRAIN_BLACKLIST, FOLDER_STATE}` from `config` using `splitFolderParts` + `join(delimiter)`, caches it module-level, returns it) and `getFolder(key)` (returns the cached value; throws a clear error if called before `resolveFolders` has run, or with an unknown key). Verify: new `test/folder-resolver.test.js` covers dot-delimiter passthrough, slash-delimiter translation, unresolved-state error, unknown-key error.
- [x] 2.3 Wire `resolveFolders(imap)` into `runInit` (`src/lib/workflows/init-workflow.js`), called before building the folder list passed to `createAppFolders`; use `getFolder(...)` for that list instead of raw `config.FOLDER_*`. Verify: `init-workflow.test.js` (add if missing) asserts `resolveFolders` runs before `createAppFolders`.
- [x] 2.4 Replace `config.FOLDER_*` with `getFolder('FOLDER_*')` in `src/lib/workflows/scan-workflow.js` (all sites: inbox open/search, spam move, alert-append, logging) and `src/lib/workflows/idle-workflow.js` (inbox lock/logging). Verify: `scan-workflow.test.js` and `idle-workflow.test.js` updated to stub `getFolder` and pass.
- [x] 2.5 Replace `config.FOLDER_*` with `getFolder('FOLDER_*')` in `src/lib/workflows/train-workflow.js` and `src/lib/workflows/map-workflow.js`. Verify: existing/updated tests for both pass with `getFolder` stubbed.
- [x] 2.6 Replace `config.FOLDER_STATE` with `getFolder('FOLDER_STATE')` in `src/lib/state-manager.js` (all three usages: read, write, delete). Verify: `state-manager` tests (new, from section 1) pass with `getFolder` stubbed.
- [x] 2.7 `grep -rn "config\.FOLDER_" src/lib/` to confirm no remaining raw folder-string usage outside `folder-resolver.js` itself and `init-workflow.js`'s pre-resolution folder list construction. Verify: grep output matches only the expected two files.
- [x] 2.8 Add a slash-delimiter end-to-end-style unit test (fake in-memory IMAP client, per the pattern used by `scan-workflow.test.js`) that runs `runInit` then a scan/train workflow against a `/`-delimited fake server and confirms folder operations use `/`-joined paths. Verify: test passes and would have failed against the pre-change code.

## 3. Per-message failure isolation — scanning (4.4)

- [x] 3.1 In `src/lib/clients/rspamd-client.js`'s `checkEmail`, attach `err.status = response.status` to the `Error` thrown on a non-OK response. Verify: `rspamd-client.test.js` asserts the thrown error has `.status === 400` (or similar) on the existing non-ok-response test.
- [x] 3.2 In `src/lib/utils/email-parser.js`'s `parseRspamdOutput`, attach `err.permanent = true` to the `Error` thrown on an invalid response shape. Verify: `email-parser.test.js` asserts `.permanent === true` on the existing invalid-format test.
- [x] 3.3 Create `src/lib/utils/error-classifier.js` exporting `isPermanentError(err)`: `true` if `err.permanent === true` or `err.status` is in `[400, 500)`; `false` otherwise (fail-safe transient default, including for plain network errors with neither property). Verify: new `test/error-classifier.test.js` covers all three branches plus the "neither property present" default.
- [x] 3.4 Rewrite `processWithRspamd` (`src/lib/services/message-service.js`) to use `Promise.allSettled` over the message map. Inside each mapped function: on success, resolve with the message-with-spamInfo object (as today); on error, if `isPermanentError(err)`, log at `warn` (uid, subject, error) and resolve with `null` (skip marker) instead of rejecting; otherwise rethrow (rejected). After `allSettled`, filter out `null`s and collect any rejections; if there are rejections, throw one aggregate `Error` summarizing the failed UIDs (batch fails for retry, matching current behavior for transient failures); otherwise return the filtered successful messages. Verify: rewritten `test/message-service.test.js` (or existing test file) covers: all-succeed (unchanged shape), one-permanent-one-success (permanent skipped, success kept, no throw), one-transient (whole call rejects), mixed permanent+transient (rejects, but the permanent one's `warn` log still fired before the throw).
- [x] 3.5 Confirm in `scan-workflow.js`'s `scanBatch` that `last_uid` is computed from the originally-fetched `messages` array (not `processedMessages`), so a permanently-skipped message's UID is still included when state advances — no code change expected here, just a verifying test. Verify: new `scan-workflow.test.js` case where `processWithRspamd` skips the highest-UID message in a batch; assert the written state's `last_uid` still equals that UID.

## 4. Per-message failure isolation — training (4.4)

- [x] 4.1 Rewrite `trainSpam`/`trainHam` (`src/lib/services/training-service.js`, via the shared `processWithRspamdLearn` path) to use `Promise.allSettled`, reusing `isPermanentError` from `error-classifier.js` with the same permanent-skip/transient-rethrow split as 3.4. On success, return the message; on permanent failure, log `warn` and omit it from the returned array; on transient failure, rethrow (aggregate error, whole call rejects). Return shape becomes `{learned: Message[]}` (array of messages actually learned). Verify: `training-service.test.js` (add if missing) covers the same four cases as 3.4, adapted for `trainSpam`/`trainHam`.
- [x] 4.2 Update `runTraining` (`src/lib/workflows/train-workflow.js`) to call `moveMessages(imap, learned, destFolder)` using the `learned` array returned by `trainFn`, instead of moving the full `batchMessages`. Verify: `train-workflow.test.js` (add if missing) confirms a permanently-failing message in a batch is (a) not moved to `destFolder` and (b) does not prevent the rest of the batch from being learned and moved.
- [x] 4.3 Confirm (via a passing test, no behavior change expected) that a transient training failure still aborts the whole `runTraining` call for that batch, preserving the orchestrator's existing step-retry-on-throw behavior. Verify: test asserts `runTraining` rejects and `moveMessages` was not called for that batch when `trainFn` rejects with a transient error.

## 5. Fix the red test suite (4.8)

- [x] 5.1 In `test/email-parser.test.js`, update `should parse Rspamd response with spam action` (action `'add header'`) to expect `isSpam: false` (only `reject` means spam per current, intentional behavior) and rename it to reflect that it's a non-spam, flagged-but-not-rejected case. Verify: `npx vitest run test/email-parser.test.js` passes.
- [x] 5.2 In `test/rspamd-maps.test.js`, update `should create a new map file with normalized emails` to expect entries in input/insertion order (normalized), not sorted order. Verify: `npx vitest run test/rspamd-maps.test.js` passes.
- [x] 5.3 In `test/rspamd-maps.test.js`, rename `should preserve sort order` to `should preserve insertion order` and update its expectation to input order (`['z@domain.com', 'a@domain.com', 'm@domain.com']`), matching `updateMap`'s documented no-sort behavior. Verify: `npx vitest run test/rspamd-maps.test.js` passes.
- [x] 5.4 Run the full suite and confirm zero failures: `npm test`. Verify: output shows `0 failed` (only the counts from all new tests added by sections 1-4 plus the existing suite, all green).

## 6. README rewrite (4.11)

- [x] 6.1 Rewrite the "Features" section: remove the "mirrored to disk" and "no external database or file storage required" claims (Redis + `SPAM_SCANNER_DATA` are required); keep the rest accurate. Verify: manual read-through against `docker-compose.yml` and `rspamd-external-storage` spec.
- [x] 6.2 Fix "Whitelist & Blacklist Maps" section: correct scores to match `rspamd/config/multimap.conf` (whitelist −20, blacklist +20) and correct folder names to `INBOX.scanner.train.whitelist` / `INBOX.scanner.train.blacklist` (dot-separated, matching `config.js` defaults). Verify: values match `multimap.conf` and `config.js` exactly.
- [x] 6.3 Fix the "Folder Structure (IMAP)" table: all folder names to their actual dot-separated `config.js` defaults (`INBOX.scanner.train.spam`, `.train.ham`, `.train.whitelist`, `.train.blacklist`, `FOLDER_STATE` default `scanner.state` — not `INBOX.scanner.state`, see 4.12); add `FOLDER_SPAM_LOW`/`FOLDER_SPAM_HIGH` rows. Verify: every value matches `src/lib/utils/config.js`.
- [x] 6.4 Fix "Environment Variables" section: correct every default to match `config.js` (see section 7 for the full 4.12 list); add the missing settings the review lists (`SPAM_PROCESSING_MODE`, `FOLDER_SPAM_LOW`/`HIGH`, `MAX_RETRIES`, `LOG_FILTER_INCLUDES`/`EXCLUDES`, all `AI_*` settings) with one-line descriptions. Verify: side-by-side diff against `.env.example` (post-6.4/7.x) shows no mismatches.
- [x] 6.5 Rewrite "Setup" / "Local Development Setup": replace `cd rspamd && docker-compose up -d` with `bin/local/rspamd.sh up` (and `down`); replace the placeholder clone URL with `git@github.com:awaragi/spam-scanner.git` (or its `https://github.com/awaragi/spam-scanner.git` form); replace `node src/init-folders.js` reference check (confirm this script still exists — keep if so); remove the empty "Setup (Legacy - for reference)" heading entirely. Verify: every command in the section is one that actually exists in `bin/local/` or `src/`.
- [x] 6.6 Fix "Docker Deployment" section: replace `docker-compose` (v1) with `docker compose` (v2) throughout; fix the "Backup Rspamd Data" commands to reference the real bind-mounted `${SPAM_SCANNER_DATA}` host directory instead of the non-existent `rspamd/docker-compose.yml`, or a plain `tar` of that directory (per `design.md`/review 4.11.h — no code change, just accurate docs of the current bind-mount reality). Verify: no reference to `rspamd/docker-compose.yml` remains anywhere in the file.
- [x] 6.7 Fix "Usage" / "Script Execution Order" sections: remove the "(placeholder for future)" notes on `train-whitelist.js`/`train-blacklist.js` (fully implemented); document IDLE mode (`SCAN_INTERVAL=0`) alongside single-run and poll mode; fix `SCAN_BATCH_SIZE` example/default references to `200`. Verify: matches `config.js` and `orchestration` spec's documented `SCAN_INTERVAL` scenarios.
- [x] 6.8 Fix "Backup & Restore" and "CLI Tools" sections: update script paths to `src/admin/*.js` (`read-state.js`, `write-state.js`, `delete-state.js`, `reset-state.js`, `uid-on-date.js`, `list-all.js`, `read-email.js`) and remove the stale `rspamd/docker-compose.yml`-based restore commands (covered by 6.6). Verify: `ls src/admin/` matches every script path referenced.
- [x] 6.9 Fix the `export $(grep -v '^#' .env | xargs)` snippet under "Development" — replace with a documented, safe alternative (e.g. `bin/local/start.sh .env <script>`, which already handles this safely) or note the fragility explicitly if kept as a fallback. Verify: manual read-through.
- [x] 6.10 Fix the "Gmail-style labels" claim in `SPAM_PROCESSING_MODE` documentation (both README and `.env.example`, see 7.x) to describe IMAP keywords accurately (not Gmail labels, which are a different, provider-specific mechanism — see review 5.22). Verify: no remaining "Gmail" reference tied to the `label` processing mode.
- [x] 6.11 Full read-through of the rewritten README against the review's 4.11.a–4.11.q table; confirm each row's "Reality" column is now what the README says. Verify: checklist walk-through, one line per sub-finding.

## 7. `.env.example` and default alignment (4.12)

- [x] 7.1 Fix `.env.example`'s `SCAN_INTERVAL` comment/value: `config.js` default is `-1` (when unset) — clarify in the comment that `300` is only an example poll value and the code's true default (when the variable is absent) is `-1`, matching the `orchestration` spec's "Configurable sleep interval" requirement. Verify: comment no longer implies `300` is what happens if the line is deleted.
- [x] 7.2 Fix `.env.example`'s `SCAN_BATCH_SIZE` value from `10000` to `200`, matching `config.js`'s actual default. Verify: `grep SCAN_BATCH_SIZE .env.example` shows `200`.
- [x] 7.3 Fix `.env.example`'s `SPAM_PROCESSING_MODE` value from `folder` to `label`, matching `config.js`'s actual default (or, if `folder` is intentionally preferred as the shipped example default going forward, flag this explicitly to the user rather than silently changing which mode new installs get — see task 7.6). Verify: resolved per 7.6's decision, then `grep` confirms the file matches it.
- [x] 7.4 Fix `.env.example`'s `AI_MODEL` value from `gpt-5-nano` to `gpt-4o-mini`, matching `config.js`'s actual default (or, if `gpt-5-nano` is intentionally the recommended shipped example, flag this explicitly rather than silently reverting — see task 7.6). Verify: resolved per 7.6's decision.
- [x] 7.5 Fix `.env.example`'s `FOLDER_STATE` value from `INBOX.scanner.state` to `scanner.state`, matching `config.js`'s actual default. Verify: `grep FOLDER_STATE .env.example` shows `scanner.state`.
- [x] 7.6 Decide (ask the user if genuinely ambiguous) whether `SPAM_PROCESSING_MODE=folder` and `AI_MODEL=gpt-5-nano` in `.env.example` are deliberate example choices that differ from `config.js`'s fallback defaults (in which case document that explicitly in the file's comments, e.g. "recommended for new installs; code default if unset is X") or accidental drift (in which case align them to `config.js`). Verify: `.env.example` comments make the relationship between the shipped example value and the code's true default unambiguous either way — no more silent disagreement.
- [x] 7.7 Fix `.env.example`'s `IMAP_TLS` comment: it's already `true` in the example, but add a comment noting the code's actual default when the variable is *absent* is `false` (finding 5.18's underlying issue is out of scope to fix in code, but the example must not imply the safe value is automatic). Verify: comment present and accurate.
- [x] 7.8 Cross-check every row of the review's 4.12 table (`FOLDER_STATE`, `FOLDER_TRAIN_*`, `SCAN_INTERVAL`, `SCAN_BATCH_SIZE`, `SPAM_PROCESSING_MODE`, `AI_MODEL`, `IMAP_TLS`) one more time against the final `.env.example` and the rewritten README (section 6) to confirm all three (code, `.env.example`, README) now agree, or where they intentionally differ, the difference is documented rather than accidental. Verify: table walk-through, one line per row.

## 8. Full verification

- [x] 8.1 Run `npm test` and confirm the full suite passes with zero failures, including all new tests added in sections 1-4. Verify: command output.
- [x] 8.2 Run `npm run format:check`; if the files touched by this change are non-conformant, run `npm run format` scoped to only the files this change modified (do not reformat unrelated files — 41 files are already non-conformant repo-wide per finding 5.17, out of scope here). Verify: `git diff --stat` shows only intentionally-touched files.
- [x] 8.3 `grep -rn "config\.FOLDER_" src/lib/` and `grep -rn "results\[0\]" src/lib/state-manager.js` one final time to confirm no regressions crept back in during later edits. Verify: grep output matches expectations from tasks 2.7 and 1.4.
- [x] 8.4 Re-read the final `README.md` and `.env.example` end-to-end as a first-time installer would, and confirm every command and default mentioned is one that exists and matches the code. Verify: manual read-through, no code changes expected from this task alone.

## 9. Post-implementation refactor: uniform `config.FOLDER_*` access (user feedback)

After section 2 shipped, the user flagged that `getFolder('FOLDER_X')` (string-keyed accessor) and `config.FOLDER_X` (direct field access, still used by `folder-processor.js`'s presence checks and the two admin scripts) were two different patterns for the same set of keys, plus a hand-maintained `FOLDER_KEYS` list in `folder-resolver.js` duplicating `config.js`'s key set. Agreed refactor: `resolveFolders(imap)` mutates `config.FOLDER_*` in place instead of populating a separate cache behind an accessor, so every call site goes back to plain `config.FOLDER_X` - the same pattern already used for every other setting.

- [x] 9.1 Rewrite `src/lib/utils/folder-resolver.js`: drop `FOLDER_KEYS`, `getFolder(key)`, `getFolders()`, and `__resetForTests()`; `resolveFolders(imap)` now loops `Object.keys(config)`, and for every key starting with `FOLDER_` whose value is a `string`, reassigns `config[key]` to its delimiter-translated form (skips non-string values rather than crashing, so a future non-path `FOLDER_*` setting is safe). Verify: `resolveFolders` still throws when no delimiter is discoverable; naturally idempotent when called twice.
- [x] 9.2 Revert every `getFolder('FOLDER_X')` call site back to `config.FOLDER_X` and drop the now-unused `folder-resolver.js` import: `src/lib/state-manager.js`, `src/lib/workflows/scan-workflow.js`, `src/lib/workflows/idle-workflow.js` (also restores its `config` import), `src/lib/workflows/train-workflow.js`, `src/lib/workflows/map-workflow.js`, `src/lib/workflows/init-workflow.js`, `src/lib/processors/folder-processor.js` (collapses its dual-pattern spot - the presence check and the `moveMessages` call now both read the same `config.FOLDER_SPAM_LOW`/`config.FOLDER_SPAM_HIGH` expression). Verify: `grep -rn "getFolder" src/` returns nothing.
- [x] 9.3 Update tests to match: `folder-resolver.test.js` rewritten to assert mutation of a mock `config` object (including a "non-string FOLDER_-prefixed value is skipped" case and an idempotency case) instead of a returned/cached map; `init-workflow.test.js` and `folder-resolution-integration.test.js` updated to read `config.FOLDER_*` (mutated by a mocked or real `resolveFolders`) instead of calling `getFolder`; `state-manager.test.js`, `scan-workflow.test.js`, `idle-workflow.test.js`, `train-workflow.test.js` drop their `folder-resolver.js` mocks entirely. Verify: `npm test` - full suite green.
- [x] 9.4 Update this change's `design.md` (4.2 decision section) and `specs/folder-resolution/spec.md` to describe the mutation-in-place design instead of the superseded `getFolder`/cache design, including the accepted trade-off (no runtime "not resolved yet" guard - relies on the `orchestration` spec's existing "one-time init phase" ordering guarantee instead). Verify: `openspec validate harden-scan-reliability-and-docs --strict` passes.
- [x] 9.5 Run `npm test` and `npx prettier --check` on every file this refactor touched. Verify: 264/264 tests pass; prettier clean.

## 10. `SCAN_INITIAL_STATE`: configurable missing-state behavior (user request)

The user pointed out that 3.1's safe-default fix (skip to `UIDNEXT - 1` when no state exists) makes it impossible to intentionally scan an entire existing inbox from scratch on first run - a real, frequently-wanted case. Added a `SCAN_INITIAL_STATE` config setting (`new` default, matching 3.1's existing behavior unchanged; `all` starts at `last_uid: 0` unconditionally) instead of requiring manual state-message surgery to get a full-inbox scan.

- [x] 10.1 Add `SCAN_INITIAL_STATE` to `src/lib/utils/config.js`: `(process.env.SCAN_INITIAL_STATE || 'new').toLowerCase()`, plus a load-time validation throwing a clear error if the value isn't `new` or `all` (same fail-fast pattern as the `AI_MODEL`-required-when-enabled check). Verify: `config.test.js` covers the default, both valid values, and the invalid-value throw.
- [x] 10.2 Update `readScannerState` (`src/lib/state-manager.js`) so the `UIDNEXT` peek (and its resulting `last_uid = UIDNEXT - 1` computation) only runs when `mailboxPath` is given AND `config.SCAN_INITIAL_STATE !== 'all'`; when `all`, skip the `imap.status` call entirely and use the caller-supplied default `last_uid` (`0`) unconditionally. Verify: `state-manager.test.js` covers `all` mode (no `imap.status` call, `last_uid: 0` even with a non-empty mailbox) alongside the existing `new`-mode cases.
- [x] 10.3 Update `openspec/changes/harden-scan-reliability-and-docs/specs/state-manager/spec.md`'s "Safe default when scanner state is missing" requirement to describe both modes and the invalid-value fail-fast behavior. Verify: `openspec validate harden-scan-reliability-and-docs --strict` passes.
- [x] 10.4 Document `SCAN_INITIAL_STATE` in `.env.example` and `README.md` next to `SCAN_BATCH_SIZE`/`SCAN_READ`, explaining it only affects the very first run before any state exists. Verify: manual read-through.
- [x] 10.5 Run `npm test` and `npx prettier --check` on every file this addition touched. Verify: full suite green; prettier clean.
