# Tasks

## 1. Auth configuration section (D1)

- [x] 1.1 Add `API_ADMIN_PASSWORD`, `API_JWT_SECRET`, `API_ADMIN_TOKEN_TTL`, `API_MAILBOX_TOKEN_TTL` to the `AppConfig` interface in `config/app-config.schema.ts`, and add a new `apiGroup: ConfigGroupDef` (title e.g. "HTTP API Auth Configuration") to `configGroups` with `API_ADMIN_PASSWORD`/`API_JWT_SECRET` as `z.string().min(1)` (no default, required — same pattern as `MAILBOX_IMAP_PASSWORD`) and both TTLs as `intField(3600)`, each with a `.describe(...)` line. Place `apiGroup` in `configGroups` before `mailboxGroup`; verify `app-config.schema.spec.ts` still passes and add a case asserting a fixture env with the four keys parses, and that omitting `API_ADMIN_PASSWORD` or `API_JWT_SECRET` fails validation naming the missing key.
- [x] 1.2 Add an `ApiAuthConfig` class to `config/app-config.ts` (fields `adminPassword`, `jwtSecret`, `adminTokenTtlSeconds`, `mailboxTokenTtlSeconds`, each read via `config.get('...', { infer: true })`), and register it in `config/config.module.ts`'s `sections` array; verify with an addition to `config.module.spec.ts` that `moduleRef.get(ApiAuthConfig)` resolves the four values from a fixture env (including the two required secrets).
- [x] 1.3 Regenerate `.env.example` via `npm run generate:env-example --workspace=app/server` and confirm the four new keys appear under the new group heading; verify `app-config.schema.env-example.spec.ts` (the drift test) still passes.

## 2. `@nestjs/jwt` dependency and token payloads (D2)

- [x] 2.1 Add `@nestjs/jwt` (a `^12` release line matching the other `@nestjs/*` deps) to `app/server/package.json` dependencies and run `npm install` from the repo root (single root lockfile); verify `npm ls @nestjs/jwt --workspace=app/server` resolves cleanly.
- [x] 2.2 Add `api/common/token.ts` defining the token payload types: an `AdminTokenPayload` (`{ sub: 'admin'; scope: 'admin' }`) and a `MailboxTokenPayload` (`{ sub: string; scope: 'mailbox'; mailboxId: string }`), plus a `TokenPayload` union and a small type-guard helper if useful; verify `nest build` compiles. (No unit test required for pure types.)

## 3. Zod validation pipe (D5)

- [x] 3.1 Add `api/common/zod-validation.pipe.ts`: a `@Injectable()` `PipeTransform` constructed with a `ZodType` schema, whose `transform(value)` runs `schema.safeParse(value)`, throws `BadRequestException` with the flattened issues on failure, and returns the parsed data on success; verify with unit tests that a valid value passes through parsed, and an invalid value throws `BadRequestException`.

## 4. Auth guards (D4)

- [x] 4.1 Add `api/common/guards/token.util.ts` (or a private method) that extracts the `Bearer` token from a request's `Authorization` header and `JwtService.verify`s it, throwing `UnauthorizedException` when the header is missing/malformed or verification fails (expired/tampered/wrong-secret); verify with unit tests covering: missing header, non-`Bearer` header, a token signed with a different secret, and a valid token returning its decoded payload.
- [x] 4.2 Add `api/common/guards/admin.guard.ts` (`AdminGuard implements CanActivate`) that verifies the token (task 4.1) then requires `payload.scope === 'admin'`, throwing `UnauthorizedException` otherwise; verify with unit tests: a valid admin token activates, a mailbox token throws `UnauthorizedException`, a missing/expired token throws `UnauthorizedException`.
- [x] 4.3 Add `api/common/guards/mailbox-scope.guard.ts` (`MailboxScopeGuard implements CanActivate`) that verifies the token, requires `payload.scope === 'mailbox'` (an admin token → `UnauthorizedException`), then requires `payload.mailboxId === request.params.mailboxId` (mismatch → `ForbiddenException`); verify with unit tests: matching mailbox token activates, a token for a different mailbox throws `ForbiddenException`, an admin token throws `UnauthorizedException`.

## 5. Auth service and controller (D3)

- [x] 5.1 Add `api/auth/auth.service.ts` (`@Injectable`, depends on `ApiAuthConfig`, `JwtService`, `MailboxRepository`): `login(password)` compares against `config.adminPassword` using a constant-time comparison (`crypto.timingSafeEqual` over length-guarded buffers; unequal lengths short-circuit to a rejection), throwing `UnauthorizedException` on mismatch and signing an admin-scope token (`expiresIn: adminTokenTtlSeconds`) on match; `exchangeForMailboxToken(mailboxId)` throws `NotFoundException` when `mailboxId` is not in `MailboxRepository.findAll()`, otherwise signs a mailbox-scope token (`expiresIn: mailboxTokenTtlSeconds`). Verify with unit tests (mock `JwtService`/`MailboxRepository`): correct password returns a token signed with admin scope/TTL; wrong password throws `UnauthorizedException` and signs nothing; exchange for a known mailbox signs a mailbox-scope token carrying that `mailboxId`; exchange for an unknown mailbox throws `NotFoundException`.
- [x] 5.2 Add `api/auth/login.schema.ts` (zod `{ password: string }`) and `api/auth/auth.controller.ts`: `POST /auth/login` (unguarded, body validated via `ZodValidationPipe`) returning the admin token, and `POST /auth/mailboxes/:mailboxId/token` (`@UseGuards(AdminGuard)`) returning the exchanged mailbox token. Verify with unit tests that the controller delegates to `AuthService` with the parsed inputs and returns its result (guards themselves are unit-tested in task group 4).
- [x] 5.3 Add `api/auth/auth.module.ts` importing `JwtModule.registerAsync` (`useFactory: (cfg: ApiAuthConfig) => ({ secret: cfg.jwtSecret })`, `inject: [ApiAuthConfig]`) and `MailboxesModule`, providing `AuthService` + the guards, exporting `JwtModule`/guards for other API modules; verify `nest build` compiles and a `Test.createTestingModule` importing it resolves `AuthService`.

## 6. RunnerRegistry and MailboxRunner additions (D6)

- [x] 6.1 Add `MailboxRunner.getSettings(): MailboxSettings` (returns `this.settings`) and `MailboxRunner.triggerInitFolders(): Promise<void>` (opens a session via the existing private `openSession`, calls `folderInitService.initFolders(session)`, `safeLogout` in `finally` — mirroring `runWithSession`'s session lifecycle but without the coalescing `runJob` wrapper, since folder init is bootstrap work); verify with unit tests (fake collaborators): `getSettings()` returns the resolved settings after a bootstrap, and `triggerInitFolders()` opens a session, calls `initFolders`, and logs out even if `initFolders` throws.
- [x] 6.2 Add `RunnerRegistry.getMailboxStatus(mailboxId): MailboxRunnerStatus`, `RunnerRegistry.getMailboxSettings(mailboxId): MailboxSettings`, and `RunnerRegistry.triggerInitFolders(mailboxId): Promise<void>`, each looking up the runner and throwing `Unknown mailbox: ${mailboxId}` (matching `triggerNow`) when absent, then delegating to the runner; verify with unit tests: each returns/delegates for a known mailbox, and throws the `Unknown mailbox` error for an unknown id. Ensure `RunnerRegistry` is exported from `runtime.module.ts` (add to `exports`) so `ApiModule` can inject it.

## 7. Mailbox admin service for state and lists (D7)

- [x] 7.1 Add `application/mailbox-admin/mailbox-admin.service.ts` (`@Injectable`, depends on `MailboxRepository`, `PinoLogger`) with a private `withConnection(mailboxId, fn)` that resolves the mailbox from `findAll()` (throws `NotFoundException` if absent), `newClient(...)` → `connect()` → `fn(imap)` → `safeLogout(...)` in `finally`; and public methods `readState(mailboxId)` (returns the scanner state, or `null` when none exists — catch the "not found" path rather than throwing), `resetState(mailboxId)` (delegates to `deleteScannerState`), `readList(mailboxId, kind)` and `replaceList(mailboxId, kind, addresses)` where `kind: 'whitelist'|'blacklist'` maps to `STATE_KEY_WHITELIST_MAP`/`STATE_KEY_BLACKLIST_MAP` via `readMapState`/`writeMapState`. Verify with unit tests using a fake IMAP client / spied repository functions: `withConnection` always logs out (even when `fn` throws), an unknown mailbox throws `NotFoundException`, `readState` returns `null` when no state exists, `resetState` deletes, and `readList`/`replaceList` hit the correct state key per kind.
- [x] 7.2 Add `application/mailbox-admin/mailbox-admin.module.ts` providing/exporting `MailboxAdminService`, importing `MailboxesModule`; verify `nest build` compiles.

## 8. Health service, rspamd probe, health controller (D8)

- [x] 8.1 Add `RspamdGateway.ping(): Promise<boolean>` — a lightweight best-effort reachability probe against rspamd (resolves `true` when reachable, `false` on any error/timeout, never throws); verify with unit tests (mocked HTTP) that a reachable rspamd returns `true` and a failing/timing-out call returns `false` without throwing.
- [x] 8.2 Add `api/health/health.service.ts` (`@Injectable`, depends on `RunnerRegistry`, `AiFailureTracker`, `RspamdGateway`): `liveness()` returns `{ status: 'up' }` only; `health()` returns `{ status, rspamd: 'reachable'|'unreachable', ai, mailboxes: [{ mailboxId, state, mode, lastSuccessfulScanAgeMs }] }` derived from `RunnerRegistry.getStatus()` (last successful scan age from `jobs.scan.lastRunAt` when `lastResult === 'success'`, else `null`) and `RspamdGateway.ping()`. Verify with unit tests: `liveness()` exposes nothing privileged; `health()` reports `unreachable` when `ping()` resolves `false` without failing; the per-mailbox summary reflects `getStatus()`.
- [x] 8.3 Add `api/health/health.controller.ts`: `GET /health/live` (unguarded, `liveness()`), and — since full health is admin-scoped per D8 — expose `health()` on the admin controller (task 9) rather than here, OR guard a `GET /health` with `AdminGuard`; pick one and keep the liveness route unguarded. Verify the liveness route needs no token and the health route is admin-guarded (delegation unit test).

## 9. Admin controller (D9, D10)

- [x] 9.1 Add `api/admin/admin.controller.ts` (`@UseGuards(AdminGuard)`): `GET /admin/mailboxes` returning `RunnerRegistry.getStatus().mailboxes`; `GET /admin/health` returning `HealthService.health()`; `GET /admin/settings` returning non-secret app settings (assemble from the injected config sections, omitting every credential/secret — no `adminPassword`, `jwtSecret`, `RSPAMD_PASSWORD`, `AI_API_KEY`, IMAP password). Verify with unit tests that each route delegates to its collaborator and that `GET /admin/settings` never includes a secret field.

## 10. Mailbox controller (D5, D6, D7, D10)

- [x] 10.1 Add `api/mailbox/job-name.ts` mapping kebab route names `scan|train-spam|train-ham|train-whitelist|train-blacklist` to the runner's `JobName`s and `init-folders` to the init-folders trigger, plus `api/mailbox/*.schema.ts` (settings-update reusing `overridableSchema`; list-replace as a validated array of address strings). Verify with a unit test that a valid kebab job name maps and an unknown one is rejected (used by the controller to raise `BadRequestException`).
- [x] 10.2 Add `api/mailbox/mailbox.controller.ts` (base path `mailboxes/:mailboxId`, `@UseGuards(MailboxScopeGuard)`): `POST .../jobs/:job/trigger` (map job name → `RunnerRegistry.triggerNow` or `triggerInitFolders`, unknown → `BadRequestException`); `GET .../status` → `getMailboxStatus`; `GET .../settings` → `getMailboxSettings`; `PUT .../settings` (body via `ZodValidationPipe`) → `updateSettings`; `GET .../state` → `MailboxAdminService.readState`; `POST .../state/reset` → `resetState`; `GET .../lists/:kind` and `GET .../lists/:kind/export` → `readList`; `PUT .../lists/:kind` and `POST .../lists/:kind/import` (body validated) → `replaceList`. Translate the registry's `Unknown mailbox` error to `NotFoundException` where the service layer doesn't already throw it. Verify with unit tests (mocked `RunnerRegistry`/`MailboxAdminService`) that each route parses/validates input and delegates correctly, and that an invalid settings/list body throws before any collaborator is called.

## 11. API module wiring (D9)

- [x] 11.1 Add `api/api.module.ts` importing `AuthModule`, `RuntimeModule`, `MailboxesModule`, `AiModule`, `RspamdModule`, and `MailboxAdminModule`, declaring the `AuthController`, `AdminController`, `MailboxController`, and health controller, and providing `HealthService` + `MailboxAdminService` as needed; ensure `AiFailureTracker` is exported from `AiModule` and `RunnerRegistry` from `RuntimeModule` (add exports where missing). Import `ApiModule` last in `app.module.ts`. Verify `nest build` compiles and `app.module.spec.ts` (or a new `api.module.spec.ts`) resolves the controllers via `Test.createTestingModule` with a fixture env providing the two required secrets.

## 12. Integration verification

- [x] 12.1 Run the full test suite (`npm test --workspace=app/server`) and `npm run lint --workspace=app/server` (oxlint --type-aware + dependency-cruiser) and confirm both pass with no regressions and no layering violations (nothing under `api/` imported by a lower layer; `api/` may import `runtime`/`application`/`infrastructure`/`config`). **Done:** 683/683 tests across 62 files pass; dependency-cruiser reports no violations (167 modules, 640 deps); `nest build` compiles. Only pre-existing, unrelated oxlint warnings remain (`domain/utils/mailboxes.spec.ts` sort-compare).
- [x] 12.2 Boot the app as a real process with the two new required secrets set (and an unreachable mailbox, same limitation as prior changes — no live IMAP in this repo) and confirm via logs: startup fails clearly when `API_ADMIN_PASSWORD` or `API_JWT_SECRET` is unset, and starts and listens on `PORT` when they are set; SIGTERM still shuts down promptly. Auth/guard/endpoint behavior is covered by the unit tests above rather than a live HTTP client (API integration tests are deferred per the proposal). **Done:** boot without secrets exits with `Config validation error: API_ADMIN_PASSWORD ... API_JWT_SECRET ...`. Boot with secrets: Nest starts and listens on PORT; `GET /health/live` → 200 `{"status":"up"}` (no token); `GET /admin/mailboxes` → 401 without a token and 200 with an admin token from `POST /auth/login`; wrong login password → 401; the unreachable mailbox's bootstrap fails and retries with backoff (~2s gap observed) without crashing the process; SIGTERM exits promptly.
- [x] 12.3 Run `openspec validate 5-server-api-auth --strict` and confirm it reports valid after implementation. **Done:** reports valid.
