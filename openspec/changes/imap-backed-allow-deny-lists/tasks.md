# Tasks

## 1. List state storage (`state-manager` capability)

- [x] 1.1 Add `readMapState(imap, mapStateKey)` to `src/lib/state-manager.js`, mirroring `readScannerState`'s folder-open/search/newest-UID-wins pattern (no `defaultState` branch needed — missing state simply returns `[]`). Reuse `parseStateFromEmail` (already `JSON.parse` wrapped in try/catch, returns `null` on failure) to detect unparseable content. Verify: `npx vitest run test/state-manager.test.js` passes.
- [x] 1.2 Add unit tests to `test/state-manager.test.js` for `readMapState`: reads back a previously written JSON list; selects the highest-UID message when duplicates exist; returns `[]` when no message matches the key; returns `[]` (not a thrown error) when the matched message's body isn't valid JSON.

## 2. Sender-list storage/matching utilities (`sender-lists` capability)

- [x] 2.1 Create `src/lib/utils/sender-lists.js` with `normalizeEmail(email)` (moved from `rspamd-maps.js`: trim, lowercase, require `@`) and `senderAddressOf(message)` (reads `message.envelope.from[0].address`, returns `null` if absent). Verify: unit tests cover normalization edge cases (case, whitespace, no `@`) and missing/empty `envelope.from`.
- [x] 2.2 Add `mergeAddresses(existing, incoming)` and `overrideAddresses(incoming)` helpers to `sender-lists.js` (both normalize + dedupe; `merge` unions with `existing`, `override` ignores it) for reuse by both training and the import script. Verify: unit tests for dedup, empty inputs, case-insensitive merge.
- [x] 2.3 Rewrite `src/lib/services/map-service.js`: drop `updateMapFile(mapPath, senders)` (local-file write); add `updateListState(imap, mapStateKey, senders, mode = 'append')` that calls `readMapState`, applies `mergeAddresses`/`overrideAddresses` per `mode`, and writes the result via `writeMapState(imap, mapStateKey, JSON.stringify(list))`. Keep `extractSenderAddresses` unchanged (it never touched files). Verify: `test/map-service.test.js` updated and passing.
- [x] 2.4 Delete `src/lib/utils/rspamd-maps.js` and `test/rspamd-maps.test.js` (fully superseded by 1.1 + 2.1–2.3). Verify: `grep -rn "rspamd-maps" src/ test/` returns nothing.

## 3. Categorization: four tiers, blacklist override, whitelist adjustment (`scan-inbox` capability)

- [x] 3.1 Add `SPAM_CLEAN_THRESHOLD` (default `30`), `SPAM_LOW_THRESHOLD` (default `60`), `SPAM_CONFIRMED_THRESHOLD` (default `200`) to `src/lib/utils/config.js`, following the `AI_ESCALATE_TO_*_THRESHOLD` pattern. Remove `RSPAMD_WHITELIST_MAP_PATH`/`RSPAMD_BLACKLIST_MAP_PATH` and the `dataDir` const (confirm via grep it has no other use before removing; drop the now-unused `path` import only if nothing else in the file needs it). Verify: `node -e "console.log(require('./src/lib/utils/config.js'))"`-style sanity check or existing config tests still pass.
- [x] 3.2 Simplify `parseRspamdOutput` in `src/lib/utils/email-parser.js` to return `{score, required}` only — drop `isSpam` (no longer derived from rspamd's `action`), `isWhitelisted` (no longer derived from the `WHITELIST_EMAIL` symbol), and `level` (already always `null`). Update its JSDoc. Verify: `test/email-parser.test.js` updated (drop the `WHITELIST_EMAIL`-symbol tests and the action-based `isSpam` assertions; keep/adjust the `score`/`required` parsing tests) and passing.
- [x] 3.3 Rewrite `categorizeMessages` in `src/lib/utils/spam-classifier.js` to take `(messages, cleanThreshold = 30, lowThreshold = 60, confirmedThreshold = 200)`, computing `scorePercentage` from each message's (already whitelist-adjusted, see 3.4) `spamInfo.score`/`spamInfo.required`, and bucketing per the spec: `confirmed` (→ `spamMessages`) when `scorePercentage >= confirmedThreshold` (checked first, independent of whitelist); else `whitelisted` (→ `whitelistedMessages`) when `spamInfo.isWhitelisted`; else `clean`/`low`/`high` by percentage as today. No `isBlacklisted`/`isSpam` field is read here — blacklist is handled upstream (3.5) as a separate, pre-rspamd path. Verify: `test/spam-classifier.test.js` rewritten for the new signature and precedence (severe content still confirms a whitelisted sender; whitelist routes to `whitelistedMessages` only when under the confirmed threshold) and passing.
- [x] 3.4 Update `processOneMessage`/`processWithRspamd` in `src/lib/services/message-service.js` to accept a `whitelistSet`, compute `isWhitelisted` via `senderAddressOf(message)` + set lookup, and attach `spamInfo: {score: rawScore - (isWhitelisted ? 20 : 0), required, isWhitelisted, subject, date}` (drop `level`/`isSpam` fields). Verify: `test/message-service.test.js` updated for the new `spamInfo` shape and the score-adjustment behavior, passing.
- [x] 3.5 In `src/lib/workflows/scan-workflow.js`'s `scanBatch`, before calling `processWithRspamd`, partition the batch by blacklist match (`senderAddressOf` + `blacklistSet` lookup): blacklisted messages skip the rspamd call and AI entirely and go straight into the `spamMessages` result; the remainder goes through `processWithRspamd(remaining, whitelistSet)` → `categorizeMessages(...)` as before, with its `spamMessages` output merged with the blacklisted ones. Verify: new tests in `test/scan-workflow.test.js` confirm a blacklisted message never reaches the mocked `processWithRspamd`/AI classification call.
- [x] 3.6 In `runScan` (`scan-workflow.js`), load `whitelistSet`/`blacklistSet` once (via `readMapState` for both keys, normalized into `Set`s) before the batch loop, and thread them into every `scanBatch` call for that run — not reloaded per batch. Pass `config.SPAM_CLEAN_THRESHOLD`/`SPAM_LOW_THRESHOLD`/`SPAM_CONFIRMED_THRESHOLD` into `categorizeMessages` (today's `scanBatch` calls it with no args at all — this activates real configurability for the first time). Verify: a test confirms the list-state read happens once per `runScan` call, not once per batch, when a scan spans multiple batches.

## 4. Training workflow: write to IMAP, not local files

- [x] 4.1 Rewrite `runMapTraining` in `src/lib/workflows/map-workflow.js`: drop the `mapPath` parameter, local-file `updateMapFile` call, `fs.readFile`-then-`writeMapState` backup dance, and `path.isAbsolute` resolution entirely. Replace with a single `updateListState(imap, mapStateKey, senders, 'append')` call (from 2.3). Update `runWhitelist`/`runBlacklist` to drop the removed `config.RSPAMD_WHITELIST_MAP_PATH`/`RSPAMD_BLACKLIST_MAP_PATH` arguments. Verify: `test/map-workflow.test.js` updated (drop the mock config's map-path entries, assert `updateListState` is called with the right key/mode) and passing.

## 5. Import script

- [x] 5.1 Create `src/admin/import-list.js` following `src/admin/uid-on-date.js`'s `yargs`/`newClient()` pattern: positional/`--list <whitelist|blacklist>` (required, validated against `config.STATE_KEY_WHITELIST_MAP`/`STATE_KEY_BLACKLIST_MAP`), `--file <path>` (required, plain CLI argument, no relation to any config/env map-path), `--mode <append|override>` (default `append`). Reads the file, splits lines, normalizes via `sender-lists.js`, calls `updateListState(imap, mapStateKey, addresses, mode)`, logs the resulting list size, and `imap.logout()` in a `finally`. Verify: run manually against a scratch text file and a test mailbox (or a temp file with 2–3 known addresses) and confirm the resulting list via `src/admin/read-state.js`-style inspection or a direct `readMapState` check.
- [x] 5.2 Add `test/admin/import-list.test.js` (first test for any `src/admin/*.js` script — no existing convention to follow, use the repo's standard `vi.mock` style for `imap-client`/`state-manager`/`fs`). Cover: `--list` selects the correct state key; `append` merges with existing IMAP-backed entries; `override` replaces them entirely and drops entries absent from the source file; running `append` twice is idempotent (no duplicates). Verify: `npx vitest run test/admin/import-list.test.js` passes.
- [x] 5.3 Pretty-print list state on write: change `updateListState` (`map-service.js`) to `JSON.stringify(list, null, 2)` instead of unindented `JSON.stringify(list)`, so the raw IMAP state message is human-readable. Verify: `test/map-service.test.js` updated to assert on the indented output; `npx vitest run test/map-service.test.js` passes.
- [x] 5.4 Add `--format <txt|json>` to `import-list.js` (default `txt`, matching current behavior). `txt` keeps the current newline-split parsing; `json` instead `JSON.parse`s the file as an array of address strings before normalizing. Verify: new tests in `test/admin/import-list.test.js` cover both formats.
- [x] 5.5 Create `src/admin/export-list.js` (same `yargs`/`newClient()`/`.env` pattern): `--list <whitelist|blacklist>` (required), `--format <txt|json>` (default `txt`), `--file <path>` (optional — write to stdout when omitted). Reads the list via `readMapState`, formats it (`txt`: newline-joined; `json`: the raw array, pretty-printed), writes to the file or stdout. Verify: run manually and confirm both formats against a real mailbox's list.
- [x] 5.6 Add `test/admin/export-list.test.js` (same mocking approach as `import-list.test.js`). Cover: `--list` selects the correct state key; `txt` format produces one address per line; `json` format produces the raw array; writes to `--file` when given, stdout otherwise; an empty list produces empty output, not an error. Also add an export→import round-trip test (export `--format json` from one mocked state, import `--format json --mode override` into another, assert the resulting list matches). Verify: `npx vitest run test/admin/export-list.test.js` passes.
- [x] 5.7 Update `README.md`'s "Migrating an existing local map file" section (and `.env.example` if relevant) to document `--format`, and add an export example for backup/cross-account migration.
- [x] 5.8 Create `src/admin/export-mailbox-state.js` (same `yargs`/`newClient()`/`.env` pattern as `export-list.js`; distinct name from the existing scanner-only `read-state.js`/`write-state.js`): `--file <path>` (optional — stdout when omitted). Reads scanner state (`readScannerState(imap)`, no default — mirrors `read-state.js`'s let-it-throw-if-missing behavior), whitelist, and blacklist (`readMapState`), and writes `{ scannerState, whitelist, blacklist }` as pretty-printed JSON. Verify: run manually against a real mailbox and inspect the output shape.
- [x] 5.9 Create `src/admin/import-mailbox-state.js`: `--file <path>` (required), `--mode <append|override>` (default `append`, applies to `whitelist`/`blacklist` only). Reads and `JSON.parse`s the bundle file, then restores only the keys present: `scannerState` (if present) via `writeScannerState` (full replace, no merge concept), `whitelist`/`blacklist` (if present) via `updateListState(..., mode)`. Verify: run manually as the reverse of 5.8's output.
- [x] 5.10 Add `test/admin/export-mailbox-state.test.js` and `test/admin/import-mailbox-state.test.js` (same mocking approach as the list scripts). Cover: export bundles all three keys correctly; import restores all three when all are present; import restores only the keys present in a partial bundle (e.g. lists only, scanner state left untouched); `--mode` is passed through to `updateListState` for both lists; an export→import round-trip (mocked) ends up with the same bundle contents. Verify: both test files pass.
- [x] 5.11 Document `export-mailbox-state.js`/`import-mailbox-state.js` in `README.md`'s import/export section and the Backup & Restore section, alongside the per-list and scanner-state-only scripts, making clear when to use which (single list vs. full mailbox state).

## 6. rspamd config and Docker cleanup

- [x] 6.1 Delete `rspamd/config/multimap.conf` (contains only the `whitelisted_email`/`blacklisted_email` rules being removed; auto-loaded from `local.d`, no other reference to clean up). Verify: `grep -rn "multimap" rspamd/` returns nothing.
- [x] 6.2 Remove the `${SPAM_SCANNER_DATA}/rspamd/maps` volume mount and `RSPAMD_WHITELIST_MAP_PATH`/`RSPAMD_BLACKLIST_MAP_PATH` environment lines from the `spam-scanner` service, and the `${SPAM_SCANNER_DATA}/rspamd/maps:/etc/rspamd/maps` mount from the `rspamd` service, in `docker-compose.yml`. Verify: `docker compose config` (or `-f docker-compose.yml config`) parses without error and shows no `maps` references.
- [x] 6.3 Remove the same `rspamd` service maps mount from `bin/local/docker-compose.yml`. Verify: `docker compose -f bin/local/docker-compose.yml config` parses cleanly.
- [x] 6.4 Remove the `rspamd/maps` directory creation and `whitelist.map`/`blacklist.map` `touch` calls from `cmd_init()` in `bin/local/rspamd.sh`. Delete `bin/local/sort-maps.sh` entirely (already flagged stale/broken in `ROADMAP.md`; its subject no longer exists). Verify: `grep -rln "maps" bin/local/` shows no remaining map-file references.
- [x] 6.5 Remove the now-stale `rspamd/maps/*.map` entry from `.dockerignore` and `rspamd/maps/` entry from `.gitignore`.

## 7. Documentation

- [x] 7.1 Update `README.md`: rewrite the whitelist/blacklist section to describe IMAP-backed, app-side matching (blacklist absolute override, whitelist score adjustment) instead of local map files and multimap; remove the `RSPAMD_WHITELIST_MAP_PATH`/`RSPAMD_BLACKLIST_MAP_PATH` lines from the `.env` example block; remove the `${SPAM_SCANNER_DATA}/rspamd/maps` line from the Data Persistence section; document `SPAM_CLEAN_THRESHOLD`/`SPAM_LOW_THRESHOLD`/`SPAM_CONFIRMED_THRESHOLD` and the `import-list.js` script (usage, `--mode` semantics, when to run it).
- [x] 7.2 Add `SPAM_CLEAN_THRESHOLD`/`SPAM_LOW_THRESHOLD`/`SPAM_CONFIRMED_THRESHOLD` to `.env.example`, styled like the existing `AI_ESCALATE_TO_*_THRESHOLD` block.
- [x] 7.3 Mark ROADMAP.md item 5.30 resolved, referencing this change.

## 8. Full verification pass (first pass)

- [x] 8.1 `npm test` (full `vitest run`) passes with no failures.
- [x] 8.2 `grep -rn "RSPAMD_WHITELIST_MAP_PATH\|RSPAMD_BLACKLIST_MAP_PATH\|rspamd-maps\|WHITELIST_EMAIL\|BLACKLIST_EMAIL" src/ test/ rspamd/ docker-compose.yml bin/` returns nothing outside of intentionally-kept historical references (e.g. `docs/features/` design docs, left as-is per design.md's non-goals).
- [x] 8.3 `npx prettier --check .` passes (or run `npm run format` to fix).
- [x] 8.4 `openspec validate imap-backed-allow-deny-lists --strict` still passes after any spec/task adjustments made during implementation.

## 9. Fix: whitelisted messages must keep their actual tier, not a forced "clean"

Found during review: `categorizeMessages` still had the pre-redesign `whitelistedMessages` bucket, which unconditionally merged any non-confirmed whitelisted message into `nonSpamMessages` for disposition - contradicting the "adjusts score rather than overriding classification" principle (a whitelisted sender scoring `high` after the `-20` discount was silently treated as `clean`, no label, no folder move).

- [x] 9.1 Rewrite `categorizeMessages` (`spam-classifier.js`) to drop the `whitelistedMessages` bucket and `isWhitelisted` branch entirely - pure 4-tier percentage logic on the (already whitelist-adjusted) score, identical for every message regardless of `spamInfo.isWhitelisted`. Verify: `test/spam-classifier.test.js` updated - a whitelisted message scoring in the `high` band is classified `high`, not merged into `nonSpamMessages`.
- [x] 9.2 Update `scanBatch` (`scan-workflow.js`) so AI eligibility, not tier assignment, is where whitelist matters: partition `categorized.nonSpamMessages`/`lowSpamMessages` by `spamInfo.isWhitelisted` before calling `classifyWithAi` (only non-whitelisted messages go to AI), then merge the whitelisted ones back into the _same_ tier they were partitioned out of (not force-merged into `nonSpamMessages`). Verify: new tests in `test/scan-workflow.test.js` confirm a whitelisted `low`-tier message skips the AI call but still ends up in `lowSpamMessages`, and a non-whitelisted message in the same batch still reaches AI.
- [x] 9.3 Redefine `whitelistedTotal` in `scanBatch`'s return value as a straight count of processed messages with `spamInfo.isWhitelisted` (across all tiers), since there's no longer a dedicated bucket to measure. Verify: existing tests referencing `whitelistedTotal` still pass with the new computation.
- [x] 9.4 Full suite green, `openspec validate --strict` clean.
