---
name: review-branch
description: Create or tear down a throwaway git branch that stages a batch of commits as one uncommitted merge, so they can be reviewed together as a single diff in the IDE's source-control panel, without touching the real working branch. Use when the user asks to "create a review branch", "let me review all the changes as a whole", "switch back to master and drop the review branch", "start a review branch", or similar - for both starting a review and cleaning one up afterward.
metadata:
  author: spam-scanner
  version: '1.0'
---

Read `skills/review-branch/SKILL.md` (repo root) now, in full, and follow it exactly as
this skill's instructions, passing along any `args` given here.
