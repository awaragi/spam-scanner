# Deliberately fixture/fake secret-shaped values - real-looking but never real -
# used to assert the migration script never surfaces a secret's value on
# stdout/stderr, even though the value legitimately appears inside the
# generated output file itself.
IMAP_HOST=imap.fixture.example.com
IMAP_PORT=993
IMAP_USER=owner@fixture.example.com
IMAP_PASSWORD=FixtureImapSecretPass789
FOLDER_STATE=INBOX.scanner.state
RSPAMD_PASSWORD=FixtureRspamdSecretHash456
AI_API_KEY=sk-fixture-SecretApiKey123
