services:
  spam-scanner:
    build: .
    image: spam-scanner
    container_name: spam-scanner
    restart: no
    env_file:
      - .env
    environment:
      - TZ=UTC
    volumes:
      - ~/.spamassassin:/var/lib/spamassassin

volumes:
  spamdata:
