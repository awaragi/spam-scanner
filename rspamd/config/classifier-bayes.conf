# Per-mailbox Bayes models: with per_user enabled, each mailbox's classification
# and training act on separate per-user models. Redis key prefix becomes %s%l%r,
# where the mailbox id is appended to the global key pattern (the migration script
# relies on this key layout to copy the global corpus to per-user keys).
backend = "redis";
per_user = true;
